<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="360dp"
    android:layout_height="800dp">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/first_nav"
        android:layout_width="360dp"
        android:layout_height="56dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/sec_nav"
            android:layout_width="296dp"
            android:layout_height="28dp"
            app:layout_constraintStart_toStartOf="@+id/first_nav"
            app:layout_constraintTop_toTopOf="@id/first_nav"
            android:layout_marginStart="16dp"
            android:layout_marginTop="14dp">

            <ImageView
                android:id="@+id/back_iv"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/arrow_left" />
            <TextView
                android:id="@+id/info_tv"
                android:layout_width="82dp"
                android:layout_height="28dp"
                android:textSize="19sp"
                android:text="계정 정보"
                android:textColor="@color/black"
                android:textStyle="bold"
                app:layout_constraintStart_toEndOf="@id/back_iv"
                android:layout_marginStart="8dp" />
        </androidx.constraintlayout.widget.ConstraintLayout>
        
        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/user"
            app:layout_constraintTop_toTopOf="@id/sec_nav"
            app:layout_constraintStart_toEndOf="@id/sec_nav"
            android:layout_marginStart="4dp"
            />


        
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>